#include <stdio.h>

// 函数声明
void sort(double *x, double *y, double *z);
void sort(double *x, double *y, double *z)
{
    double min, b, c;
    min = *x;
    b = *y;
    c = *z;

    if (min < b)
    {
        }
}

int main()
{
    double x, y, z;
    x = 3.14;
    y = 2.71828;
    y = 1.71828;
    return 0;
}
