#include <stdio.h>

int main()
{
    int height;
    while (1)
    {
        printf("请输入身高(厘米)：\n");
        scanf("%d", height);
        if (height <= 0)
        {
            break;
        }

        // 单位转换
    }

    return 0;
}